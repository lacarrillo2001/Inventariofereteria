@model IEnumerable<InFerreteria.Models.ProveedorDto>
@{
    ViewData["Title"] = "Proveedores";
}
<div class="container mt-4">
    <div class="d-flex align-items-center mb-3">
        <h3 class="mb-0 me-3">Proveedores</h3>
        <div class="ms-auto d-flex gap-2">
            <a asp-action="Create" class="btn btn-primary">Nuevo</a>
            <a asp-action="Inactivos" class="btn btn-outline-secondary">Ver inactivos</a>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }

    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                
                <th>Nombre</th>
                <th>Contacto</th>
                <th>Correo</th>
                <th>Estado</th>
                <th style="width:260px">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var p in (Model ?? Enumerable.Empty<InFerreteria.Models.ProveedorDto>())
                        .Where(x => x.Activo))
            {
                <tr>
                    <td>@p.Nombre</td>
                    <td>@p.Contacto</td>
                    <td>@p.Correo</td>
                    <td>@Html.Raw("<span class='badge bg-success'>Activo</span>")</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@p.Id" class="btn btn-sm btn-outline-info">Detalles</a>
                        <a asp-action="Edit" asp-route-id="@p.Id" class="btn btn-sm btn-warning">Editar</a>

                        <form asp-action="Inactivar" method="post" class="d-inline">
                            <input type="hidden" name="id" value="@p.Id" />
                            <button type="submit" class="btn btn-sm btn-outline-secondary">Inactivar</button>
                        </form>

                        <form asp-action="Delete" method="post" class="d-inline" onsubmit="return confirm('¿Eliminar definitivamente?');">
                            <input type="hidden" name="id" value="@p.Id" />
                            <button type="submit" class="btn btn-sm btn-danger">Eliminar</button>
                        </form>
                    </td>
                </tr>
            }
            @if (!(Model?.Any(m => m.Activo) ?? false))
            {
                <tr>
                    <td colspan="5" class="text-center text-muted">No hay proveedores activos.</td>
                </tr>
            }

        </tbody>
    </table>
</div>
